import React from 'react';
import { View } from 'react-native';

interface SeparatorProps {}

const Separator: React.FunctionComponent<SeparatorProps> = () => <View style={{ height: 10 }} />;

export default Separator;
